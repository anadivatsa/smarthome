__pycache__/
*.pyc
venv/
*.egg-info/

# secrets — copy *.example and fill in
*.env
*.env.local
spotify_tokens.json

# runtime state
tags.json
presence.json

# Wake word binary models (device-trained, not committed)
*.ppn
*.onnx

# Contain live API key — never commit
KEY_URLS.txt
SIRI_SHORTCUTS.md

# Runtime data — never commit DB or backups
data/
